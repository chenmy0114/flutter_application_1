name: flutter_application_1
description: A new Flutter project.

publish_to: 'none' # Remove this line if you wish to publish to pub.dev

version: 0.0.1+1

environment:
  sdk: '>=2.19.4 <4.0.0'

dependencies:
  flutter:
    sdk: flutter

  # 本地化支持（用于日期选择器等显示本地语言）
  flutter_localizations:
    sdk: flutter

  english_words: ^4.0.0
  provider: ^6.0.0
  sqflite: ^2.0.0+3
  path: ^1.8.3
  sqflite_common_ffi: ^2.2.0
  fl_chart: ^1.1.1
  shared_preferences: ^2.1.1
  math_expressions: ^2.4.0  # 算式解析
  decimal: ^2.3.3           # 精度处理
  encrypt: ^5.0.1
  clipboard: ^3.0.8
  crypto: ^3.0.3  # 用于生成加密密钥
  convert: ^3.1.1 # 数据格式转换
dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^2.0.0

flutter:
  uses-material-design: true